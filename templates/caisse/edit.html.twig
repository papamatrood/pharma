{% extends 'layout/layout.html.twig' %}

{% block title %}Edit Caisse{% endblock %}

{% block body %}
    <div class="containerBackground">
    <h1>Edit Caisse</h1>

    {{ include('caisse/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('caisse_liste') }}">back to list</a>

    {{ include('caisse/_delete_form.html.twig') }}
    </div>
{% endblock %}
