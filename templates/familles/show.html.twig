{% extends 'base.html.twig' %}

{% block title %}Familles{% endblock %}

{% block body %}
    <h1>Familles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ famille.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ famille.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('familles_index') }}">back to list</a>

    <a href="{{ path('familles_edit', {'id': famille.id}) }}">edit</a>

    {{ include('familles/_delete_form.html.twig') }}
{% endblock %}
